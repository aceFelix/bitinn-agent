<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itniuma.bitinn.mapper.interaction.ArticleFavoriteMapper">

    <insert id="insert">
        INSERT INTO article_favorite (article_id, user_id, create_time)
        VALUES (#{articleId}, #{userId}, NOW())
    </insert>

    <delete id="delete">
        DELETE FROM article_favorite WHERE article_id = #{articleId} AND user_id = #{userId}
    </delete>

    <select id="findByArticleAndUser" resultType="com.itniuma.bitinn.pojo.entity.ArticleFavorite">
        SELECT id, article_id, user_id, create_time
        FROM article_favorite
        WHERE article_id = #{articleId} AND user_id = #{userId}
    </select>

    <select id="findFavoritedArticleIdsByUserId" resultType="java.lang.Integer">
        SELECT article_id FROM article_favorite WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="countByArticleId" resultType="java.lang.Long">
        SELECT COUNT(*) FROM article_favorite WHERE article_id = #{articleId}
    </select>

</mapper>
